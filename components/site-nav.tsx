'use client';
import Link from 'next/link'; import {useState} from 'react';
export function SiteNav({lang,onLang}:{lang:'ES'|'EN';onLang:()=>void}){const [open,setOpen]=useState(false);return <header className="site-header"><div className="nav-shell"><button className="brand-button" onClick={()=>setOpen(!open)} aria-label="Abrir o cerrar navegación" aria-expanded={open}><img src="/images/logo-andariegos.png" alt="Andariegos Cocina de Mundo"/></button><div className={'nav-drawer '+(open?'open':'')}><nav className="main-nav"><Link href="/" onClick={()=>setOpen(false)}>INICIO</Link><Link href="/menu" onClick={()=>setOpen(false)}>MENÚ</Link><Link href="/simijaca" onClick={()=>setOpen(false)}>SIMIJACÁ</Link><Link href="/reservas" onClick={()=>setOpen(false)}>RESERVAS</Link><Link href="/cuenta" onClick={()=>setOpen(false)}>MI CUENTA</Link></nav></div><div className="nav-controls"><button onClick={onLang} className="round-control" aria-label="Cambiar idioma">{lang==='ES'?'EN':'ES'}</button><button onClick={()=>{document.documentElement.classList.toggle('dark');localStorage.setItem('andariegos-theme',document.documentElement.classList.contains('dark')?'dark':'light')}} className="round-control" aria-label="Cambiar tema"><span className="leaf">🍃</span><span className="wood">🪵</span></button><Link href="/menu" className="round-control" aria-label="Carrito">🛒</Link></div></div></header>}
