"use client";
import { useState } from "react";
const cards=[["Inicio","Qué está pasando en la web","Contenido destacado, temporada y avisos."],["Pedidos","Solicitudes","Preparado para conectar pedidos reales."],["Reservas","Calendario","Preparado para solicitudes y confirmación."],["Menú","Carta","Productos, precios, ingredientes y disponibilidad."],["Contenido","Historias","Fotos, textos y bloques editables."],["Ambiente","Hoja / Madera / Temporada","Previsualizar antes de publicar."]];
export default function AdminPage(){
  const [preview,setPreview]=useState(false);
  return <main className="admin-page"><section className="admin-head"><p className="eyebrow">ESCRITORIO DE ANDARIEGOS</p><h1>Admin</h1><p>Una interfaz sencilla para editar, previsualizar y publicar sin tocar el código.</p><div className="admin-actions"><button className="button" onClick={()=>setPreview(v=>!v)}>{preview?"CERRAR VISTA PREVIA":"VER VISTA PREVIA"}</button><button className="button button-primary">GUARDAR BORRADOR</button></div></section><section className="admin-grid">{cards.map(([title,subtitle,text])=><article key={title} className="admin-card"><span>{subtitle}</span><h2>{title}</h2><p>{text}</p><button>ABRIR →</button></article>)}</section>{preview&&<section className="admin-preview"><div><p className="eyebrow">VISTA PREVIA</p><h2>Así se vería antes de publicar.</h2><p>El sistema final permitirá guardar borradores, previsualizar y publicar sin mostrar cambios parciales al público.</p></div><div className="preview-paper"><span>ANDARIEGOS</span><b>Una cocina que viaja.</b><small>Vista previa de contenido</small></div></section>}</main>;
}
