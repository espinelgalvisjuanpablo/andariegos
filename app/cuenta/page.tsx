"use client";
import { useState } from "react";
import { useLanguage } from "@/components/language-provider";
export default function AccountPage(){
  const {lang}=useLanguage(); const en=lang==="EN"; const [sent,setSent]=useState(false);
  return <main className="inner-page"><section className="inner-hero compact"><p className="eyebrow">{en?"YOUR SPACE":"TU ESPACIO"}</p><h1>{en?"My account":"Mi cuenta"}</h1><p>{en?"Optional, simple and without passwords. Use your phone when you want to save addresses or repeat an order.":"Opcional, simple y sin contraseñas. Usa tu teléfono cuando quieras guardar direcciones o repetir un pedido."}</p></section><section className="section account-section"><div className="account-card">{!sent?<><span className="account-mark">✦</span><h2>{en?"Enter with your phone":"Entra con tu teléfono"}</h2><p>{en?"We will send you a 6-digit code by text message to confirm your access.":"Te enviaremos un código de 6 dígitos por mensaje de texto para confirmar tu ingreso."}</p><input className="text-input" placeholder={en?"Phone number":"Número de teléfono"} inputMode="tel"/><button className="button button-primary full-button" onClick={()=>setSent(true)}>{en?"CONTINUE":"CONTINUAR"}</button><small>{en?"You can also order or reserve as a guest.":"También puedes pedir o reservar como invitado."}</small></>:<><span className="account-mark">✓</span><h2>{en?"Code requested":"Código solicitado"}</h2><p>{en?"The access code will arrive here when the account connection is enabled.":"Cuando conectemos el acceso real, el código llegará por mensaje de texto."}</p><button className="button full-button" onClick={()=>setSent(false)}>{en?"CHANGE NUMBER":"CAMBIAR NÚMERO"}</button></>}</div></section></main>;
}
